# TEST_RESULTS.md — Test Plan Execution Report

**Executed:** 2026-06-01  
**Environment:** macOS, Docker Compose, Python 3.13 (local pytest)

---

## Summary

| Layer | Result | Notes |
|-------|--------|-------|
| L0 Acceptance gate | **PASS** | `docker compose up` (api + detection + dashboard); smoke script green |
| L1 API smoke | **PASS** | All endpoints 200; `scripts/smoke_test.sh` |
| L2 Detection unit | **PASS** | 8/8 `tests/test_detection.py` |
| L3 Integration | **PASS** | Footfall ↔ events; unique `event_id`; re_entry ≤ entry |
| L5 Production | **PASS** | API healthcheck; detection writes `events/*.jsonl` |
| L7 Integrity | **PASS** | Metrics differ with/without events; GMV from CSV |
| L4 Ground-truth video | **NOT RUN** | Manual labels still recommended before submit |

**Automated tests:** `31 passed, 0 failed`

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest tests/ -v
```

---

## L0 — Acceptance gate

| ID | Check | Result |
|----|-------|--------|
| A0.1 | `docker compose up -d --build` | PASS — api, detection, dashboard running |
| A0.2 | `GET /health` | PASS — `status: ok`, `rows_loaded: 101` |
| A0.3 | `GET /metrics` | PASS |
| A0.4 | `events/*.jsonl` | PASS — generated by detection container |
| A0.5 | `DESIGN.md`, `CHOICES.md` | PASS |

---

## L1 — API smoke (`scripts/smoke_test.sh`)

```
PASS  health 200
PASS  metrics 200
PASS  funnel 200
PASS  anomalies 200
PASS  events 200
PASS  DESIGN.md + CHOICES.md present
PASS  events/*.jsonl exists
```

---

## L2 — Detection unit tests

All passed after re-entry logic fix (spatial match to recent exit; unique monotonic `event_id`).

---

## L3 — Integration (post-detection run)

Sample event counts after pipeline fix:

| event_type | count |
|------------|-------|
| entry | 30 |
| exit | 43 |
| re_entry | 13 |

- `re_entry ≤ entry`: **PASS**
- `event_id` uniqueness (86 events, 86 unique ids): **PASS**
- `estimated_footfall` == entry count in `/metrics/summary`: **PASS**
- Funnel `Footfall` stage == entry count: **PASS**

---

## L7 — Integrity

| Test | Result |
|------|--------|
| Metrics differ empty vs populated `EVENTS_OUTPUT` | PASS |
| GMV changes when CSV rows removed | PASS |
| `conversion_rate_pct` derivable from customers/footfall | PASS |

---

## Fixes applied during execution

1. **Re-entry bug** — `_check_re_entry` no longer assigns every new centroid to the next recent exit; requires spatial proximity.
2. **Duplicate `event_id`** — monotonic `EVT000000001` style ids.
3. **Funnel monotonicity** — `basket_started`, `converted`, `repeat` capped so stage counts never increase down-funnel.
4. **Docker** — `.dockerignore` reduces build context; dashboard serves pre-built `dist/`; api + detection build successfully.
5. **Stale events** — prior noisy runs moved to `events/archive/`.

---

## Recommended before submission

1. **L4:** Label one short clip; compare `/events/summary` to manual counts; note margin in `CHOICES.md`.
2. **Re-run full stack on a clean machine:** `docker compose up --build`.
3. **Run:** `pytest tests/ -v` and `./scripts/smoke_test.sh`.
4. **10-minute rehearsal** per `TEST_PLAN.md` §11.

---

## Commands reference

```bash
docker compose up -d --build
./scripts/smoke_test.sh
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest tests/ -v
```
